Prject initialization cmds : "python -m venv venv" for windows  ,for linux "python3 -m venv venv"  

then : to activate virtual env : "venv\Scripts\Activate.ps1" for windows ps , for linux "source venv/bin/activate"

to upgrade pip in both os : "python -m pip install --upgrade pip"

to install all the requirements in both os :   "pip install -r requirements.txt"